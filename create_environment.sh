#!/bin/bash
# todo: save user in some dotfile
# make a shared folder for profiles
# make special function to hook (just like in netbox objects customize)
# create profile script
# change profile vim macro
